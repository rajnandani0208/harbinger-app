import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
    },
    viewButton: {
        margin: 16,
        marginVertical: 20
    },
});
export default styles; 